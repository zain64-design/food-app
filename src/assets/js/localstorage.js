// local storage only stores as string if we want to pass any object or array we need to convert first into string and then at the calling time we need to convert the string into actual type of data like array or an object.

// const userSettings = {theme: 'dark', fontFamily: 'Arial', fontSize: 25}

// const settingString = JSON.stringify(userSettings);
// console.log(typeof settingString);

// localStorage.setItem('userSettings',settingString);

// const uSetting = JSON.parse(localStorage.getItem('userSettings'));

// console.log(typeof uSetting);

// cart
//user setting
//filters
// form data


